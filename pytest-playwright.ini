[pytest]
addopts = -ra
testpaths = python_playwright/tests
python_files = test_*.py
markers =
    cart_session: 仅在登录状态文件存在时为购物车主流程加载该状态
    cart_smoke: 单一登录上下文的低频购物车主链路；不计入完整 30 条回归
    html_contract: 服务端 HTML 契约层；不起浏览器、不分端，结论确定应保持全绿
